# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Python project. No code exists yet — only scaffolding (`.gitignore`, `.env`).

## Stack hints (from .gitignore)

The `.gitignore` covers Django, Flask, Celery, Scrapy, Marimo, and standard Python tooling. Actual stack TBD as code is added.

## Environment

Secrets and config go in `.env` (already git-ignored).

## Правила общения

- Общаться с пользователем только на русском языке.
- Отвечать коротко — максимум 5 предложений.
- Беречь токены, не генерировать лишнего.
- Всегда спрашивать, если что-то непонятно или вопрос спорный.
